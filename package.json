{
  "name": "mipo-vibes",
  "private": true,
  "version": "0.1.0",
  "description": "MIPO Weighting Calculator monorepo with API and Angular frontend",
  "scripts": {
    "build": "npm run build --workspaces",
    "lint": "npm run lint --workspaces",
    "test": "npm run test --workspaces"
  },
  "workspaces": [
    "api",
    "web"
  ],
  "engines": {
    "node": ">=20.0.0",
    "npm": ">=9.0.0"
  }
}

